import type { ChatRequest, ChatApiResponse } from '@/types/api';

// Placeholder: replace with actual Claude / AI SDK integration
export async function runAgent(request: ChatRequest): Promise<ChatApiResponse> {
  return {
    message: "Placeholder agent response. AI integration coming soon.",
    products: [],
    intent: 'unknown',
  };
}
